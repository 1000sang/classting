/**
 * DB 접속정보
 *  debug: true | false,
 *           environment: 'dev' | 'production' | 'some_environment',
 *           release: null,
 *           logLevels: ['debug'],
 */
export interface HttpConfig {
  port: number;
}
